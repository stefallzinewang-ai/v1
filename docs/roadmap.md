# 路线图与里程碑

> 系统很大，原则是**先把「AI→光模块」这一条主线端到端跑通**，再横向扩展。
> 每个阶段都应是「可运行、可验证」的闭环，而非半成品堆叠。

## 阶段 0 · 架构与骨架 ✅（当前）
- [x] 八层分层架构与文档（`docs/`）
- [x] 代码包骨架与统一接口（`src/alpharadar/`）
- [x] 示例主线配置（`config/themes/ai_optical_module.yaml`）
- [x] CLI 骨架与冒烟测试

**交付物**：可 `python -m alpharadar.cli --help`；分层接口清晰，可分别填充。

## 阶段 1 · 数据打通（data）🚧 进行中
- [x] 标准 schema 与代码工具（`data/schema.py`）：统一列定义、symbol/secid 转换。
- [x] `EastmoneySource`：日线行情、指数行情、业绩报表（含公告日）、行业成分；
      解析逻辑（纯函数）与 HTTP 抓取分离，解析逻辑已离线样本单测。
- [x] `data/http.py`：限速 + 指数退避重试（4xx 不无谓重试）。
- [x] `store.py`：parquet 按年分区读写 + SQLite 水位线（往返已测）。
- [x] `ingest.py`：增量更新（按水位线）+ 行情一致性校验。
- [x] CLI `sync <theme>`：按主线候选股采集行情/财务并落地。
- [ ] 宏观 `macro()` 接入（东方财富 datacenter）。
- [ ] DuckDB 查询加速（可选优化）；缓存原始响应（可选）。
- [ ] 在能联网的机器上实跑：拉取光模块板块全历史，校验数据质量。

**验证**：解析/存储/增量逻辑已离线测试通过（13 项）；真实联网采集需在可访问
东方财富的机器上运行 `alpharadar sync ai_optical_module`（云环境因白名单代理被拦截）。

## 阶段 2 · 市场状态（regime）🚧 进行中
- [x] 四维状态打标签规则版 baseline（纯函数，合成数据离线单测）：
      趋势(均线位置+斜率) / 风格(成长vs价值) / 风险偏好(小盘vs大盘) / 流动性(占位)。
- [x] 趋势多数表决平滑，抑制抖动；各维度缺数据时优雅降级。
- [x] 指数 secid 修正 + `sync` 自动采集指数 + CLI `regime` 命令。
- [ ] 流动性维度待宏观 `macro()` 接入后实现 easing/tightening。
- [ ] 回放历史，画出 15 年状态时间线，人工核对关键拐点（如 2015、2018、2020）。

**验证**：分类逻辑已离线测试（12 项）；CLI `regime` 已用合成指数端到端跑通。
历史回放核对需真实指数数据（在可联网机器 `sync` 后运行）。

## 阶段 3 · 因子与基本面（factors + fundamental）🚧 进行中
- [x] 因子预处理管线：`standardize`（去极值+zscore/rank+方向对齐）、`cross_section_score`。
- [x] 动量因子 `compute_momentum`（价格类，横截面）。
- [x] 公司基本面打分 `FundamentalScorer`：成长（营收/净利同比+加速度）、质量（毛利率/ROE）；
      严格 point-in-time（按 ann_date 公告日切片）；按主线 emphasis 加权。
- [x] CLI `score` 命令：候选池基本面打分排序。
- [ ] 估值因子（PE/PB 分位，需补市值/估值数据）。
- [ ] 景气因子（行业出货/订单），供阶段 4 激活判断与阶段 6 融合。
- [ ] 因子有效性检验：IC / 分层回测（依赖阶段 1 真实历史）。

**验证**：标准化/合成/动量/打分已离线测试（11 项）；CLI `score` 端到端跑通，
point-in-time 正确（公告日前后取不同报告期）。全套 46 项通过。

## 阶段 4 · 主线映射（theme）🚧 进行中
- [x] YAML 产业链加载与访问器（瓶颈环节、关键变量、景气权重、板块代码）。
- [x] 激活判断 `evaluate()`：市场状态 / 行业景气 / 政策支持 三道门，带可解释理由。
      接入阶段 2 的 `RegimeState`；景气未就绪时不否决但标注待补。
- [x] 候选池 `resolve_pool()`：YAML 种子 ∪ 行业成分（按板块代码/名称关键词），纯函数可测。
- [x] CLI `theme` 命令：展示「结构 → 激活判断 → 候选池」完整收敛链。
- [ ] 瓶颈环节景气自动打分（依赖阶段 3 景气因子）。
- [ ] 多主线时按 regime + 景气自动推荐主题。

**验证**：示例主线已离线测试（9 项）并端到端跑通——市场状态 bull|growth|high
正确激活，候选池从 3 只种子自动增强到 4 只。

## 阶段 5 · 规律挖掘（mining）⭐ 核心 🚧 框架完成
- [x] IC 加权法（可解释，非黑箱）：状态内各因子 IC → 带符号权重，归一化。
- [x] Walk-forward 样本外验证框架（只用过去训练、未来验证），报告样本内/外 IC。
- [x] `Pattern` / `PatternLibrary`（JSON 存取）；`Pattern.score` 加权打分。
- [x] `build_training_panel`：从行情向量化构建因子面板/状态/前瞻收益（point-in-time）。
- [x] CLI `mine` 命令；规律库接入 `run` 流水线（叠加「规律信号」alpha 维度）。
- [ ] 防过拟合补全：纳入退市股、扣交易成本、剔除停牌/涨跌停（需真实数据）。
- [ ] 加入财务类因子（成长/质量，按 ann_date 对齐）到训练面板。
- [ ] 在真实 15 年数据上训练并核对各状态权重的经济解释力。

**验证**：框架已离线测试（合成数据中 signal 因子被正确赋高权重，样本外 IC 为正）；
`mine` 端到端跑通（20 只合成股，样本内 IC 0.22 / 样本外 0.19）。
⚠️ 真实有效性必须在真实 15 年数据上验证——这是系统可信与否的关键。

## 阶段 6 · 选股（selection）🚧 进行中
- [x] `Selector`：按维度权重融合成总分，取 Top N，保留 breakdown + 可解释理由。
- [x] `pipeline.run_pipeline`：端到端编排（市场状态→激活→候选→打分→选股→报告）。
- [x] CLI `run` 命令（支持 `--as-of` / `--save`）。
- [ ] 回测（backtest）：Point-in-time 事件回放，统计收益/胜率/回撤（见下）。

**验证**：端到端流水线已离线跑通——`run ai_optical_module` 在 bull|growth|high 状态下
正确激活并输出 Top 2（中际旭创居首），报告逻辑链完整可追溯。

## 阶段 6b · 回测（backtest）🚧 进行中
- [x] 事件回放回测 `BacktestEngine`：每期按当期状态打分→Top K 等权→前瞻收益→净值曲线。
- [x] 指标：年化/超额/IR/胜率/最大回撤 + 分市场状态超额 + 净值曲线。
- [x] 规律库驱动打分（或默认动量基线）；非重叠持有期。
- [x] CLI `backtest` 命令 + UI 回测区块（净值曲线）。
- [ ] 滚动重训做严格样本外回测（避免样本内偏乐观）。
- [ ] 扣交易成本、剔除停牌/涨跌停、纳入退市股（需真实数据）。

**验证**：回测引擎已离线测试（5 项）——有效因子跑赢基准、无效因子无超额；
`mine`→`backtest` 端到端跑通。⚠️ 当前为样本内，真实评估需滚动重训 + 真实数据。

## 阶段 7 · 报告（report）🚧 进行中
- [x] `ReportBuilder`：渲染市场状态→激活→瓶颈→候选（含逐条理由）→免责的完整 Markdown。
- [x] 接入 `run` 流水线，支持保存到 `reports/output`。
- [ ] 升级 Jinja2 模板 + 图表；标注更细的数据出处。

**验证**：`run --save` 生成完整、可读、可追溯的研究报告（已离线跑通）。

## 阶段 9 · 图形界面（ui）🚧 已上线
- [x] Streamlit 研究面板 `app.py`：市场状态/激活/瓶颈/候选(图表+理由)/报告下载。
- [x] 演示数据 `demo.py` + CLI `demo`/`ui`，离线即可体验全流程。
- [x] AppTest 端到端渲染测试。
- [ ] 后续：历史状态时间线图、因子 IC 可视化、回测曲线。

## 阶段 10 · 扩展（后续）
- [ ] 横向扩展更多主线（半导体、医药、新能源…）。
- [ ] 政策文本 NLP 自动抽取。
- [ ] 定时任务与增量更新自动化。

---

## 优先级建议

如果要尽快看到价值，推荐顺序：**阶段 1 → 2 → 4 → 5 → 6 → 3/7**。
即先打通数据与状态，把主线和规律挖掘这两个「最有信息量」的环节做出来，
再补全因子细节与报告美化。

## 风险与注意

- 🔴 **过拟合**是头号风险，阶段 5 的样本外纪律决定系统成败。
- 🟠 数据源稳定性：AKShare 接口可能变动，需在阶段 1 把缓存与重试做扎实。
- 🟠 幸存者偏差：阶段 1 务必纳入退市股，否则后续全部失真。
- 🟢 范围蔓延：克制住「一次做全市场」的冲动，先把一条主线做到极致。
