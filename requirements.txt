# ── 主线雷达 依赖 ──────────────────────────────────────────────
# 数据源（A股）
akshare>=1.12.0          # 主数据源：行情/财务/宏观/行业，免费
# tushare>=1.2.89        # 可选补充数据源（需 token）
# baostock>=0.8.8        # 可选补充：历史行情

# 数据处理
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0

# 规律挖掘 / 因子建模
scikit-learn>=1.3.0

# 存储
SQLAlchemy>=2.0.0        # 关系型元数据
pyarrow>=14.0.0          # parquet 列存（时间序列）
duckdb>=0.9.0            # 本地 OLAP 查询

# 配置 / CLI / 报告
PyYAML>=6.0
jinja2>=3.1.0            # 报告模板

# 可视化（报告用）
matplotlib>=3.7.0

# 测试
pytest>=7.4.0
